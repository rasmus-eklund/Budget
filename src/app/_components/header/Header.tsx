import Login from "./Login";

const Header = () => {
  return (
    <header className="flex justify-end items-center h-14 border-b bg-white">
      <Login />
    </header>
  );
};

export default Header;
